import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  title: {
    fontSize: 30,
    fontWeight: "bold",
    textTransform: "uppercase",
    color: "#0673BC",
    textAlign: "center",
    paddingBottom: 20,
  },
});
